defaults:
  - marl_damage_aware_always_dig
  - _self_

env_config:
  # Override default attributes to include simfire data layers.
  # NOTE: We are excluding wind_speed and wind_direction!
  attributes: [fire_map, agent_pos]
